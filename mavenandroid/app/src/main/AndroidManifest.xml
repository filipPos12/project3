<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="scan.di.uoa.gr.exampleappsoftdev">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ButtonActivity"
            android:label="@string/title_activity_button" />
        <activity
            android:name=".WelcomeActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".IntroActivity" />
        <activity android:name=".ShutdownSaActivity" />
        <activity android:name=".InsertJobActivity" />
        <activity android:name=".DeleteJobActivity" />
        <activity android:name=".SaStatusActivity" />
        <activity android:name=".SaResultsActivity" />
        <activity android:name=".SaJobsActivity" />
        <activity android:name=".AllSaResultsActivity"></activity>
    </application>

</manifest>
